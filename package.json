{
    "name": "portfolio",
    "version": "1.0.0",
    "author": "Pulkit",
    "description": "Pulkit Gupta's Portfolio",
    "scripts": {
        "format": "yarn prettier --write .",
        "build": "esbuild ./public/css/main.css  --bundle --outfile=build/build_out.css --loader:.jpeg=file && esbuild ./public/js/main.js  --bundle --outfile=build/build_out.js"
    },
    "license": "ISC",
    "dependencies": {
        "esbuild": "^0.17.12"
    },
    "devDependencies": {
        "prettier": "2.8.4"
    }
}
